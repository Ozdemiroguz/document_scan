# Keep the published archive focused on the package + the example's Dart code.

# Build artifacts, tool caches, and IDE project files — never belong in a
# published package. A `flutter test` run leaves a multi-MB .dill cache under
# build/, and IntelliJ/Android Studio drop a *.iml that's noise on the Source tab.
build/
.dart_tool/
*.iml
.idea/

# Raw screen-recording GIFs kept locally as the source for the demo animations.
# Only the optimized doc/*.webp derived from them ship — a raw GIF is 10–40 MB
# and would dwarf the whole archive.
*.gif

# Generated dartdoc HTML output (`dart doc` writes it to doc/api/). It's build
# output, not source — 180+ files that bloat the archive and are noise on the
# Source tab. The demo doc/*.webp images still ship (they're README images);
# only the generated api/ subtree is excluded.
doc/api/

# The example's platform scaffolding (Xcode/Gradle projects, app icons, launch
# storyboards) is only needed to *run* the demo locally — it's noise on the
# pub.dev "Source" tab and bloats the archive. Exclude it; the example's lib/,
# pubspec.yaml and README still ship so the demo code is browsable.
example/ios/
example/android/
example/linux/
example/macos/
example/windows/
example/web/
example/build/
example/.dart_tool/
example/integration_test/
example/test/
