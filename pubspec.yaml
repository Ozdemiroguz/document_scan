name: document_scan
description: "Composable, native-light document scanner for Flutter: document corner detection plus perspective correction, for both live camera frames and still images."
version: 0.2.1
homepage: https://github.com/Ozdemiroguz/document_scan
repository: https://github.com/Ozdemiroguz/document_scan
issue_tracker: https://github.com/Ozdemiroguz/document_scan/issues

topics:
  - document-scanner
  - computer-vision
  - camera
  - opencv
  - image-processing

environment:
  sdk: ^3.12.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  # Pure-Dart image decode/encode + perspective warp + filters (Processor).
  image: ^4.5.4
  # @internal / other annotations (pure Dart, no transitive weight).
  meta: ^1.16.0
  # Pure-Dart PDF generation, used by DocumentProcessor when the caller picks
  # ScanOutputFormat.pdf — a scan encodes to a single-page A4 PDF instead of an
  # image. No native code involved.
  pdf: ^3.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.oguzhan.document_scan
        pluginClass: DocumentScanPlugin
      ios:
        pluginClass: DocumentScanPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
